# API Configuration for Driver PWA
# For local development (localhost)
# VITE_API_BASE=http://localhost:8010

# For Docker environment - uses Vite's proxy, so use relative paths
# VITE_API_BASE=/api

# Leave empty to use relative /api proxy (recommended for Docker)
# VITE_API_BASE=
